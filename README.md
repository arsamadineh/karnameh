# کارنامه (KarNama)

**نرم‌افزار بومی و متن‌باز مدیریت پروژه، کارها و چک‌لیست‌های روزانه — با پشتیبانی کامل از زبان فارسی و چینش راست‌به‌چپ**

یک برنامه دسکتاپ و موبایل بومی (Native) ساخته شده با Tauri 2، SolidJS و SQLite. داده‌ها کاملاً محلی هستند و هیچ‌گاه به هیچ سروری ارسال نمی‌شوند.

---

## قابلیت‌های اصلی

- **داشبورد یکپارچه** — نمایش لحظه‌ای وضعیت پروژه‌ها، کارها و پیشرفت چک‌لیست‌های روزانه
- **مدیریت مشتریان** — ثبت اطلاعات تماس، رنگ اختصاصی و بودجه تجمیعی برای هر مشتری
- **کنترل پروژه‌ها** — دسته‌بندی پروژه‌ها بر اساس وضعیت، اولویت و مشتری مرتبط
- **سامانه وظایف (بورد کانبان)** — پیگیری کارها با جریان کاری کانبان
- **چک‌لیست روزانه** — ثبت وظایف روز به روز با نوار پیشرفت و ناوبری زمانی
- **تم روشن / تاریک / سیستم** — سازگار با تنظیمات رنگ سیستم‌عامل
- **بروزرسانی خودکار** — دریافت و نصب نسخه‌های جدید در پس‌زمینه بدون نیاز به دخالت کاربر
- **پشتیبان‌گیری کامل** — خروجی و ورودی اطلاعات در قالب JSON
- **داده محلی** — تمامی اطلاعات در پایگاه‌داده SQLite محلی ذخیره می‌شود

## پلتفرم‌های پشتیبانی شده

| پلتفرم | فرمت |
|---|---|
| ویندوز | `.msi` / `.exe` |
| macOS | `.dmg` (Universal — Intel + Apple Silicon) |
| لینوکس | `.deb` / `.AppImage` |
| اندروید | `.apk` |

---

## نصب

آخرین نسخه پایدار را از بخش [Releases](https://github.com/arsamadineh/karnameh/releases) دریافت کنید و فایل متناسب با سیستم‌عامل خود را نصب کنید.

---

## پیش‌نیازهای توسعه

برای ساختن پروژه به صورت محلی به موارد زیر نیاز دارید:

- **Node.js** نسخه LTS یا بالاتر
- **Rust** نسخه Stable
- ابزارهای بومی سیستم‌عامل (مانند Build Tools ویندوز یا Xcode برای macOS)

---

## راه‌اندازی محلی

```bash
# ۱. نصب وابستگی‌های Node
npm install

# ۲. اجرا در حالت توسعه (دسکتاپ)
npm run tauri dev

# ۳. ساخت فایل نصبی دسکتاپ
npm run tauri build

# ۴. ساخت APK اندروید
npm run tauri android build --apk
```

---

## ساختار پروژه

```
karnameh/
├── src/                    # پوسته کاربری (SolidJS + TypeScript)
│   ├── features/           # بخش‌های اصلی برنامه (dashboard, clients, ...)
│   ├── components/         # کامپوننت‌های قابل استفاده مجدد
│   ├── lib/                # لایه API و ابزارها
│   ├── store/              # مدیریت وضعیت سراسری
│   └── styles/             # توکن‌های طراحی و سبک‌های سراسری
├── src-tauri/              # هسته پردازشی (Rust)
│   ├── src/
│   │   ├── commands/       # دستورات IPC Tauri
│   │   ├── repositories/   # دسترسی به پایگاه‌داده
│   │   ├── models/         # مدل‌های داده
│   │   └── services/       # منطق کسب‌وکار
│   ├── migrations/         # مهاجرت‌های پایگاه‌داده SQLite
│   └── tauri.conf.json     # پیکربندی Tauri
└── .github/workflows/      # ساخت و انتشار خودکار (CI/CD)
```

---

## مشارکت در توسعه

از مشارکت شما استقبال می‌کنیم. لطفاً قبل از ارسال Pull Request، مسئله (Issue) مربوطه را ابتدا ثبت کنید.

## پروانه

این پروژه تحت مجوز **MIT** منتشر شده است.
